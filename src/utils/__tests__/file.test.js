/* eslint-env jest */

describe('utils/file', () => {

  describe('importNetcanvas(filePath)', () => {
    it.todo('rejects with a readable error when permissions are wrong');
    it.todo('rejects with a readable error when it cannot extract a protocol');
    it.todo('resolves to a uuid path in temp');
  });
});
