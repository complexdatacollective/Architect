/* eslint-disable import/prefer-default-export */

const getScreensMessage = state => state.ui.screens.message;
const getScreensStack = state => state.ui.screens.screens;

export {
  getScreensMessage,
  getScreensStack,
};
