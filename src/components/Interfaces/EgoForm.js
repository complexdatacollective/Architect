import {
  Name,
  Form,
  IntroductionPanel,
} from '../../sections';

const template = {
};

const EgoForm = {
  sections: [
    Name,
    IntroductionPanel,
    Form,
  ],
  template,
};

export default EgoForm;
