import {
  Name,
  Form,
  IntroductionPanel,
} from '@components/sections';

const template = {
};

const EgoForm = {
  sections: [
    Name,
    IntroductionPanel,
    Form,
  ],
  template,
};

export default EgoForm;
