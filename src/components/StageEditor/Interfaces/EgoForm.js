import {
  Form,
  IntroductionPanel,
} from '@components/sections';

const template = {
};

const EgoForm = {
  headerSections: [
  ],
  sections: [
    IntroductionPanel,
    Form,
  ],
  template,
};

export default EgoForm;
