import {
  Name,
  Form,
  IntroductionPanel,
} from '@components/sections';

const template = {
};

const EgoForm = {
  headerSections: [
    Name,
  ],
  sections: [
    IntroductionPanel,
    Form,
  ],
  template,
};

export default EgoForm;
