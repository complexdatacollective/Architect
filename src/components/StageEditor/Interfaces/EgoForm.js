import {
  Name,
  Form,
  IntroductionPanel,
} from '../../sections';

const template = {
};

const EgoForm = {
  sections: [
    Name,
    Form,
    IntroductionPanel,
  ],
  template,
};

export default EgoForm;
