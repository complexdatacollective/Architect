import {
  Name,
  NodeType,
  Filter,
  Form,
  IntroductionPanel,
} from '../../sections';

const AlterForm = {
  sections: [
    Name,
    NodeType,
    Filter,
    IntroductionPanel,
    Form,
  ],
};

export default AlterForm;
