import {
  Name,
  FilteredNodeType,
  Form,
  IntroductionPanel,
} from '@components/sections';

const AlterForm = {
  headerSections: [
    Name,
    FilteredNodeType,
  ],
  sections: [
    IntroductionPanel,
    Form,
  ],
};

export default AlterForm;
