import {
  Name,
  NodeType,
  Form,
  IntroductionPanel,
} from '../../sections';

const AlterForm = {
  sections: [
    Name,
    NodeType,
    Form,
    IntroductionPanel,
  ],
};

export default AlterForm;
