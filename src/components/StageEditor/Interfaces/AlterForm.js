import {
  Name,
  FilteredNodeType,
  Form,
  IntroductionPanel,
} from '@components/sections';

const AlterForm = {
  sections: [
    Name,
    FilteredNodeType,
    IntroductionPanel,
    Form,
  ],
};

export default AlterForm;
