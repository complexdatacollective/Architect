import {
  FilteredNodeType,
  Form,
  IntroductionPanel,
} from '@components/sections';

const AlterForm = {
  headerSections: [
    FilteredNodeType,
  ],
  sections: [
    IntroductionPanel,
    Form,
  ],
};

export default AlterForm;
