import {
  Name,
  NodeType,
  Form,
  IntroductionPanel,
} from '../../sections';

const AlterForm = {
  sections: [
    Name,
    NodeType,
    IntroductionPanel,
    Form,
  ],
};

export default AlterForm;
