import {
  Name,
  EdgeType,
  Form,
  IntroductionPanel,
} from '../../sections';

const template = {
};

const AlterEdgeForm = {
  sections: [
    Name,
    EdgeType,
    Form,
    IntroductionPanel,
  ],
  template,
};

export default AlterEdgeForm;
