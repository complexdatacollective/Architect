import {
  Name,
  EdgeType,
  Filter,
  Form,
  IntroductionPanel,
} from '../../sections';

const template = {
};

const AlterEdgeForm = {
  sections: [
    Name,
    EdgeType,
    Filter,
    IntroductionPanel,
    Form,
  ],
  template,
};

export default AlterEdgeForm;
