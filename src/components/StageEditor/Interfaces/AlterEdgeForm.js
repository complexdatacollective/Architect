import {
  Name,
  EdgeType,
  Form,
  IntroductionPanel,
} from '@components/sections';

const template = {
};

const AlterEdgeForm = {
  sections: [
    Name,
    EdgeType,
    IntroductionPanel,
    Form,
  ],
  template,
};

export default AlterEdgeForm;
