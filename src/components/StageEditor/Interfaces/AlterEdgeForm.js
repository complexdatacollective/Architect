import {
  Name,
  FilteredEdgeType,
  Form,
  IntroductionPanel,
} from '@components/sections';

const template = {
};

const AlterEdgeForm = {
  headerSections: [
    Name,
    FilteredEdgeType,
  ],
  sections: [
    IntroductionPanel,
    Form,
  ],
  template,
};

export default AlterEdgeForm;
