import {
  Name,
  FilteredEdgeType,
  Form,
  IntroductionPanel,
} from '@components/sections';

const template = {
};

const AlterEdgeForm = {
  sections: [
    Name,
    FilteredEdgeType,
    IntroductionPanel,
    Form,
  ],
  template,
};

export default AlterEdgeForm;
