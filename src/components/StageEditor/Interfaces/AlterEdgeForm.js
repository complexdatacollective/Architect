import {
  FilteredEdgeType,
  Form,
  IntroductionPanel,
} from '@components/sections';

const template = {
};

const AlterEdgeForm = {
  headerSections: [
    FilteredEdgeType,
  ],
  sections: [
    IntroductionPanel,
    Form,
  ],
  template,
};

export default AlterEdgeForm;
