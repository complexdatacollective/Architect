import {
  Name,
  NodeType,
  Form,
  IntroductionPanel,
} from '../sections';

const PerAlterForm = {
  sections: [
    Name,
    NodeType,
    Form,
    IntroductionPanel,
  ],
};

export default PerAlterForm;
