import {
  FilteredNodeType,
  IntroductionPanel,
  DyadCensusPrompts,
} from '@components/sections';

const DyadCensus = {
  headerSections: [
    FilteredNodeType,
  ],
  sections: [
    IntroductionPanel,
    DyadCensusPrompts,
  ],
};

export default DyadCensus;
