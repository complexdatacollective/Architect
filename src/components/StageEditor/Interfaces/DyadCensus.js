import {
  Name,
  FilteredNodeType,
  IntroductionPanel,
  DyadCensusPrompts,
} from '@components/sections';

const DyadCensus = {
  sections: [
    Name,
    FilteredNodeType,
    IntroductionPanel,
    DyadCensusPrompts,
  ],
};

export default DyadCensus;
