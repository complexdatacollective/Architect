import {
  Name,
  FilteredNodeType,
  IntroductionPanel,
} from '@components/sections';

const DyadCensus = {
  sections: [
    Name,
    FilteredNodeType,
    IntroductionPanel,
  ],
};

export default DyadCensus;
