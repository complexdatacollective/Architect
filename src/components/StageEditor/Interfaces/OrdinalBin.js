import {
  Name,
  FilteredNodeType,
  OrdinalBinPrompts,
} from '@components/sections';

const OrdinalBin = {
  headerSections: [
    Name,
    FilteredNodeType,
  ],
  sections: [
    OrdinalBinPrompts,
  ],
};

export default OrdinalBin;
