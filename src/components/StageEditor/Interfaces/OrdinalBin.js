import {
  Name,
  FilteredNodeType,
  OrdinalBinPrompts,
} from '@components/sections';

const OrdinalBin = {
  sections: [
    Name,
    FilteredNodeType,
    OrdinalBinPrompts,
  ],
};

export default OrdinalBin;
