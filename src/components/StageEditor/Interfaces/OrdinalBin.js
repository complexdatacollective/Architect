import {
  FilteredNodeType,
  OrdinalBinPrompts,
} from '@components/sections';

const OrdinalBin = {
  headerSections: [
    FilteredNodeType,
  ],
  sections: [
    OrdinalBinPrompts,
  ],
};

export default OrdinalBin;
