import {
  Name,
  NodeType,
  NameGeneratorListPrompts,
} from '../sections';

const NameGeneratorList = {
  sections: [
    Name,
    NodeType,
    NameGeneratorListPrompts,
  ],
};

export default NameGeneratorList;
